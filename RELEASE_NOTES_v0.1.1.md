# Radio Echo v0.1.1

这是基于 `v0.1.0` 的 Android 稳定性与交互更新，继续作为免费公开预览版发布，仅提供 `arm64-v8a` APK。

## 核心更新

- **FT8 时序更稳健：** 发射前等待上一接收周期完成处理，避免解码尚未结束就抢占 PTT，并区分“接收收尾”“发射预备”和真实发射状态。
- **IC-705 WLAN 状态修复：** 控制、CI-V 和音频链路分别监测，断开与重连会完整清理远端会话，降低只剩音频但无法控制电台的概率。
- **CW 操作增强：** 新增默认关闭的自动 CQ，可选择 `3` 秒或 `5` 秒轮间等待；恢复 BI4SCC、BG2TFW 国内 Spot 服务器预设。
- **日志与 LoTW：** LoTW 历史同步移到日志页，支持全量、续传和增量进度；新增默认关闭、仅适用于新完成 FT8 通联的自动上传选项，其他日志仍需人工审核。
- **界面与性能：** 工作区按需加载页面，日志拆分为列表、地图和 DXCC；地图大数据量自动聚合，设置和窄屏 CW 重新布局，保存日志后提供即时提示。

## 已知限制

- FT8 普通模式对时钟偏差和部分蓝牙音频抖动的容差仍在继续验证；复杂多信号及连续自动通联不能等同于 WSJT-X/JTDX。
- FX-4CR 蓝牙 PTT 释放时间存在设备和链路波动，首次发射仍应在低功率、受控环境监督进行。
- IC-705 与 FX-4CR 以外的设备尚未完成正式兼容验收。
- FT8 自动上传默认关闭；LoTW 接收的日志通常无法撤回，启用前必须确认呼号、时间、频率和通联流程正确。
- 当前仅提供 Android `arm64-v8a`，不提供 iOS、32 位或 x86 版本。

## 安装包

```text
文件：Radio-Echo-v0.1.1-android-arm64.apk
包名：cn.bi1rre.radioradioecho
版本：0.1.1 (2)
最低 Android：API 24
目标 Android：API 35
SHA-256：f6d94ab5833b490cc90fffbc7825fec03085a66beea370b554184ab1ba987038
签名证书 SHA-256：c2fad4dc5c79163f23fb80d97427c70f4a187be714546d890bf6ceea2bb819af
```

## 升级提示

从 `v0.1.0` 升级前建议导出配置和完整 ADIF。不要卸载旧版，使用同包名、同签名 APK 直接覆盖安装；升级后检查呼号、设备配置、日志数量、LoTW 同步游标和证书状态。

本版本仍为 **Pre-release**。自动发射必须由具备相应资格的操作员持续监督，并保留从电台实体控件立即停止发射的方式。
