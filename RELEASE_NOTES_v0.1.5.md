# Radio Echo v0.1.5

这是面向户外操作体验和 FX-4CR 新蓝牙固件的维护版本，继续作为免费 Android `arm64-v8a` 公开预览版发布。

## 核心更新

- **户外工作台：** 顶部集中显示电台、频率、模式、收发和有效表计，四个主要工作区改为底部导航，并提供跨页面发射停止入口。
- **界面与输入：** 使用 RF 蓝、琥珀频率和语义状态色；修复日志表单滑动闪烁、CW 字段高度和呼号/信号报告键盘问题。
- **FX-4CR 蓝牙：** 修复版固件可自动启用频率、模式、PTT 与 PWR/SWR 回读；CW Keyer 交接后直接读取 `RM1/SM`，不再依赖可能延后的瞬时 TX 状态。旧固件仍安全降级为只写控制。
- **模块生命周期：** FT8 接收和蓝牙音频只在 FT8 工作区运行，避免 CW、日志等页面无意占用解码器和 SCO 音频链路。

## 已知限制

- FX-4CR 表计依赖支持 `RM1/SM` 回读的修复版固件；不同固件的稳定性仍可能不同。
- IC-705 USB FT8 连续自动发射、FT-DX10 USB FT8 频谱形态和部分实验性 USB 电台仍需更多真机样本。
- 当前仅提供 Android `arm64-v8a`，不提供 iOS、32 位或 x86 版本。

## 安装包

```text
文件：Radio-Echo-v0.1.5-android-arm64.apk
包名：cn.bi1rre.radioradioecho
版本：0.1.5 (6)
最低 Android：API 24
目标 Android：API 35
SHA-256：0e631cfdd7796ddafaebb65950ef0cf7b0e998c0b76510de0247c4e4dbe11610
签名证书 SHA-256：c2fad4dc5c79163f23fb80d97427c70f4a187be714546d890bf6ceea2bb819af
```

升级前建议导出配置和完整 ADIF。不要卸载旧版，使用同包名、同签名 APK 直接覆盖安装。本版本仍为 **Pre-release**，自动发射必须由具备相应资格的操作员持续监督。
